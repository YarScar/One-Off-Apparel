# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
.next/
out/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Credentials (defense-in-depth — raw credential files should live outside the repo)
*service-account*.json
*credentials*.json
*-key.json
*.pem
# Google service account default naming: <project-id>-<hex>.json
/lp-internal-ai-*.json

# AWS
.aws/

# Claude Code local settings
.claude/settings.local.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test coverage
coverage/
.nyc_output/

# Terraform sandboxes (aws-mcp-server per-job working dirs)
.tf-*

# Prisma generated client (regenerated by `pnpm db:generate`)
packages/db/generated/

# Sentry
.sentryclirc

# Local Claude Code config (skills, settings) — not shared with the team
.claude/
.opencode

# Raw grant source material (Drive exports). Large, and it holds funder
# correspondence and unredacted student data — never commit it.
packages/grants/data/
data/

# Python bytecode from the parity regeneration scripts in packages/grants/scripts/
__pycache__/
